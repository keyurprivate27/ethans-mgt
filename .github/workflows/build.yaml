name: Test Workflow

on:
  push:
  workflow_dispatch:

jobs:
  hello:
    if: github.ref == 'refs/heads/master' 
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello Radhe Krishna..."

      - name: Working Directory
        run: pwd

      - name: List All Files
        run: ls -la
